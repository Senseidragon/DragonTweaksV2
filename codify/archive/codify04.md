# Codify Session Snapshot

## Metadata
- Created at: Thu Jun 25 08:39:05 EDT 2026
- Project root: C:\Users\sense\Desktop\DragonTweaksV2
- Command invoked: /codify
- Additional user arguments: none

## Current Project Status

- Branch `advisor-persona-grounding` (unchanged mechanically this session; nothing committed; git access for the assistant remains revoked — and per a new standing instruction this session, restoration is not to be raised or asked about at all going forward, regardless of progress).
- This snapshot covers everything since `codify03.md`, which ended at the "pause before starting to implement" checkpoint. Since then, three substantially different threads were worked: (1) diagnosing and fixing the test-suite's actual runtime cost, (2) completing four paused memory updates plus a UTF-8-enforcement follow-up with a permission-boundary detour, and (3) resolving a Markdoc-to-clean-prose conversion convention for `docs/wiki-ref/` and using it to build and wire in the first real MineColonies player-facing lore entry.
- The two design-only proposals deferred at the end of `codify03.md` (#5 — the bare `"see"` keyword overlap in `ToolCallOrchestrator`'s `location` category; #6 — the persona 4th-wall slip, target ambiguous within a 3-item bundle) were **never revisited this session** — still exactly as paused, no new information about them.
- Test suite: root-caused the ">90s every run" complaint to one class (`AdvisorPersonaGenerativeTest`, sequential live OpenRouter calls — 97% of JUnit execution time), fixed via parallelization, not exclusion. Measured improvement confirmed via `build/test-results/test/*.xml` timing data and a realistic `--rerun`-forced wall-clock comparison (not a full `--rerun-tasks` rebuild, which would have confounded the measurement — this mistake was made once, caught, and redone correctly).
- Memory updates: all four items paused at the end of `codify03.md` were completed (git-access wording, UTF-8 standing requirement, domain-retrieval architecture note, wiki-ref reclassification), then a fifth follow-up implemented an actual UTF-8 validation check in `scripts/memsearch-refresh.ps1` — blocked twice by a `scripts/` permission deny rule, succeeded only after Dragon adjusted `.claude/settings.local.json` directly (the exact change made to that file was not inspected by the assistant — inferred only from the retry succeeding).
- MineColonies lore pipeline: resolved a Markdoc-tag-to-clean-prose convention against `docs/wiki-ref/needs/food.mdoc`, discovered along the way that the dev-facing Milvus memory corpus (`.memsearch/memory/domains/minecolonies/approved/`) already has a confirmed, systemic version of the defect this convention fixes (cross-referenced building/worker names rendered as bare unresolved ids), confirmed this is dev-facing and lower-priority per Dragon's correction, then built and fully wired in `docs/minecolonies-lore/needs/food.md` as the first real MineColonies entry in the actual player-facing pipeline (`docs/*-lore` → `syncLoreFromDocs` → JAR resources → `LoreIndex.java`). Verified via test-run log that `LoreIndex` now loads 92 entries (was 91).
- No git commands were run this session (consistent with the standing revocation). No commits.

## Current Task List

Internal task list (IDs #1–#10, created earlier in the broader session, before `codify03.md`):
- #1–#4: completed (cavern-classification fix, persona list-formatting fix, build-tool spawn gate, test run + audit-trail — all pre-`codify03.md`).
- #5: Propose fix for "see" keyword overlap (design only) — **still pending, not touched this session.**
- #6: Propose fix for persona 4th-wall slip (design only) — **still pending, not touched this session**; the ambiguity over which of 3 bundled backlog items this actually targets (noted in `codify03.md` Decisions Made #12) remains unresolved.
- #7–#10: completed (the four memory updates) — confirmed done both in the task tracker and in this session's actual file edits.
- No task exists yet in the tracker for the work done this session (test-suite fix, UTF-8-enforcement implementation, wiki-ref/lore pipeline work) — none were created for it.

## Session Topics Developed

**Thread 1 — Test-suite runtime:**
1. Answered a direct question ("how many tests?") from cached knowledge of the last run: 96 tests, 0 failures.
2. Dragon raised test-suite runtime (>90s every `./gradlew test`) as "increasingly irritating," asked to set aside "presumptive-passing" stable tests from "fluid" ones.
3. Diagnosed via `build/test-results/test/*.xml` timing data: not a "many tests" problem — `AdvisorPersonaGenerativeTest` alone was 76.19 of 78.66s total JUnit time (97%); every other suite combined was ~2.47s.
4. Proposed `@Tag`-based exclusion of the live test from the default `test` task, with a separate task for full/live runs — flagged that this would change what CLAUDE.md's literal "`./gradlew test` must pass" Code Change Gate wording actually covers, and asked which changes should still require the live task.
5. Dragon rejected the exclusion approach outright: "don't bother with altering the tests then... We need to actively resolve the time-consuming issues rather than let them linger... More effort needs to be directed as solving these issues."
6. Read `OpenRouterService.java` to rule out an avoidable inefficiency (retries, redundant probing) before concluding the cause was test-side serialization, not the service. Found single-threaded internal executors but assessed them as not a meaningful bottleneck at this call volume (parsing/callback work is sub-10ms; the cost is network wait, which those executors don't serialize).
7. Found the actual mechanism: `setUpAndRunTrials()`'s `@BeforeAll` ran 10 independent trials in a sequential `for` loop, each blocking on two live API calls (`handleQuery` + a separate "judge" call) before the next trial started — up to 20 fully serialized round-trips with no real dependency forcing that order.
8. Implemented: replaced the sequential loop with `CompletableFuture.supplyAsync` per trial against a `newFixedThreadPool(trialCount)`, collecting results via `.get()` after submitting all futures. Same 4 `@Test` methods, same assertions, same `INTENTS`/`TRIALS_PER_INTENT`/`PASS_RATE_THRESHOLD` — only execution strategy changed.
9. First measurement attempt used `./gradlew test --rerun-tasks`, which forced the entire NeoGradle/compile pipeline to redo, confounding the wall-clock comparison (stayed ~97s due to unrelated rebuild cost) — caught this mistake, re-measured correctly via `./gradlew test --rerun` (forces only the `test` task, leaves the rest at normal up-to-date state).
10. Confirmed result: `AdvisorPersonaGenerativeTest` suite time 76.19s → 15.81s (~4.8x); realistic `./gradlew test` wall-clock ~97s → 30.6s; 96 tests, 0 failures, both before and after.
11. Appended a `test-audit-trail.md` entry documenting the diagnosis, fix, and measured before/after.
12. Dragon asked whether a `/codify` was necessary before moving to outstanding issues — assistant answered no (full session context retained; codify is for cross-session continuity), recommended one consolidated snapshot at a natural stopping point. Dragon proceeded with "yes, go ahead" rather than requesting a codify at that point.

**Thread 2 — Resuming and completing the paused memory updates, then a UTF-8-enforcement follow-on:**
13. Began resuming the 4 paused memory-update tasks (#7–#10); read `feedback_git_access_revoked.md`'s current content as a first step.
14. Dragon interjected mid-task: **"pause. do not lose progress."** Stopped immediately (per the standing mid-task-interjection guidance), reported that nothing was at risk — only a read had occurred, no edit yet, and all prior session work was already durably saved to disk regardless of pausing.
15. Dragon: **"complete the memory updates, then pause again."** Completed all four:
    - `feedback_git_access_revoked.md`: updated `description` frontmatter and added a 2026-06-25 dated paragraph superseding the prior "tentative... might reconsider" framing — Dragon's exact words quoted ("git access will not be restored while bugs remain in current branch. stop asking."), with an explicit instruction never to raise/ask about restoration again, regardless of progress.
    - `project_open_items.md`, three sections each got a dated 2026-06-25 addendum (append-only, nothing erased): the Memsearch-encoding item gained the UTF-8-input standing requirement; the runtime-domain-retrieval item gained the Milvus-dev-only/in-JAR-player-facing architecture note plus the unrecalled "simplified index/retrieval system" as the concrete open lookup; the `docs/wiki-ref/` item was reclassified from "stalled, do not touch without authorization" to "active pending task," lifting the 2026-06-21 gate.
16. Paused as instructed; reported completion; asked if Dragon wanted to proceed to the still-untouched #5/#6 design proposals.
17. Dragon instead asked a verification question: **"have we implemented that all input to either memory or kb is mandated to be UTF-8 compliant?"** Answered no — policy/memory only, no enforcement anywhere. Verified `scripts/memsearch-refresh.ps1` already had a pre-existing `$env:PYTHONUTF8 = "1"` (not added this session) addressing the original display-crash bug specifically, distinct from input-validity enforcement.
18. Dragon: **"can this implementation be completed with relative haste? if so, do it and then pause for follow-up question."** Investigated scope: `memsearch` itself is an external CLI tool, not source living in this repo (confirmed by reading the full `memsearch-refresh.ps1` — it shells out to `memsearch index`/`memsearch search` as external commands); the only in-repo ingestion boundary is the approved-path list in that script. Auto-memory writes (this assistant's own Write/Edit tool calls) are already UTF-8 by harness default — nothing to implement there. No in-JAR knowledge base exists yet — nothing to enforce there either.
19. Attempted to add a strict UTF-8 validation pre-check (`System.Text.UTF8Encoding` with `throwOnInvalidBytes`) before the `memsearch index` call in `memsearch-refresh.ps1` — **blocked**: "File is in a directory that is denied by your permission settings." Read `claude-links/guardrail-boundary-handling.md` and followed it exactly: reported the blocked action and boundary, gave Dragon the exact manual-paste snippet, did not attempt a Bash/PowerShell workaround.
20. Dragon: **"you are authorized to make that change to the memsearch-refresh script."** Retried — same block. Reported that this is a settings-level deny rule, not a conversational permission, and verbal authorization alone doesn't override it; gave Dragon two options (apply manually, or adjust the deny rule themselves).
21. A system reminder noted Dragon opened `.claude/settings.local.json` in the IDE; Dragon said **"try again."** Retried — succeeded this time. The exact change Dragon made to that settings file was not inspected.
22. Attempted to verify the change by running a standalone PowerShell snippet that recursively read and byte-validated every file across all approved memory directories — **rejected** by Dragon via the tool-denial mechanism with feedback "fix command and retry" (the concern was the bulk-read scope itself, not a syntax error). Re-scoped to a narrow syntax-only check (`[System.Management.Automation.Language.Parser]::ParseFile`, no file content read) — passed ("Syntax OK").
23. Reported the edit was applied and syntax-verified, explicitly not run end-to-end (would trigger `docker compose up` and a live, forced Milvus reindex — not done without being asked).
24. Dragon: **"yes, update it"** (the `project_open_items.md` UTF-8 entry) — added a 2026-06-25 "implemented (partial)" paragraph documenting exactly what was done, the permission-boundary detour, and the verification-status caveat.
25. Dragon: **"you forgot I had follow-up questions."** Acknowledged directly, invited the question, took no further action until it arrived.
26. Dragon: **"we have hardened the UTF-8 rule... We have not actually verified that no non-compliant data has not already made it into existing memory or KB, correct?"** Confirmed: correct, the hardening is prospective only; distinguished actual encoding-validity risk from the original (already-mitigated) display-crash bug, which involved valid UTF-8 the Windows console couldn't print — different problem, no bearing on whether stored data is itself valid. Noted that auditing existing data would require the same kind of bulk read Dragon had just rejected, and offered a narrower filenames-only version or to hold off.
27. Dragon: **"I want to determine if this is a task that can be -effectively- assigned to subagents, or if that is about as helpful as throwing the task to a pack of wild lemurs."** Verdict: lemurs — the UTF-8 validity check is deterministic and zero-judgment; a direct decode loop does it instantly with zero hallucination risk, so spinning up LLM subagents would add cost/latency/risk for no benefit. Noted subagents earn their keep on memory/KB work that's actually judgment- or synthesis-heavy, not mechanical pass/fail checks.

**Thread 3 — Resolving the wiki-ref Markdoc convention and building the first MineColonies lore entry:**
28. Dragon: **"ok, so we can inline that particular task. Now, same general question for the wiki-ref/mdoc processing?"** Different verdict given: good fit for subagents (judgment-heavy content transformation, splits cleanly into independent per-file units) — but flagged a prerequisite: the Markdoc-tag-handling convention isn't resolved yet, and dispatching agents before resolving it risks each inventing its own inconsistent rendering for the same tag types (the actual "lemur" risk here, distinct from the UTF-8 case).
29. Dragon: **"resolve the Markdoc convention on one file first."** Read `claude-links/web-memory-ingestion-pipeline.md` (the governing 9-stage pipeline: Raw deep scrape → Cleaned reference → Short advisor artifact → Optional flavor → Metadata → Candidate submission → Validation → Promotion → Reindex/flush). Globbed `docs/wiki-ref/**/*.mdoc` (62 files across `buildings/`, `items/`, `needs/`, `systems/`). Chose `needs/food.mdoc` (already referenced in existing project memory) as the representative file. Globbed the Minecraft domain's memory tree looking for an existing raw→cleaned precedent — found only `approved/` and `deprecated/` tiers, no surviving "cleaned" intermediate examples. Read `approved/pillager.md` (distilled-advisor-block format) and `deprecated/pillager.md` (the actual deprecate-and-rebuild precedent: old raw firecrawl snapshot, Trivia-only, superseded) to establish house style. Read `buildings/kitchen.mdoc`, `buildings/cook.mdoc`, `buildings/baker.mdoc`, `buildings/farmer.mdoc` to resolve `needs/food.mdoc`'s cross-referenced building/worker tags — discovered building display names diverge from file IDs (`kitchen`→"Cookery", `cook`→"Restaurant", `baker`→"Bakery"), and that no worker-name registry exists in the capture at all (workers' display names are inferred via best-effort Titlecase, explicitly flagged as not grounded, unlike buildings).
30. Produced a written, 10-rule Markdoc-construct-to-clean-prose convention table (self vs. cross-reference building/worker tags, unresolvable item/research-link tags, bare dynamic tags like `food_list` — flagged as content gaps, never fabricated — paired vs. bare GUI-block tags, tables/blockquotes/bold kept as-is, images/internal links stripped to plain mentions) and applied it to produce a full "cleaned reference" (pipeline stage 2) text for `needs/food.mdoc`, with the `food_list` gap explicitly flagged rather than invented.
31. Asked an open question: no existing "cleaned" stage directory convention exists for any domain — where should staged artifacts live?
32. Dragon: **"vlrsn staged directory != clean files like in minecraft-lore or existing memory entries?"** Investigated precisely: Globbed and read `docs/minecraft-lore/hostile/pillager.md` (confirmed `pipeline_stage: advisor-artifact` — a stage-3+ final artifact, not stage-2) and `MINECRAFT_LORE_INDEX.md`. Then Globbed `.memsearch/memory/domains/minecolonies/**/*` and discovered (previously unknown to the assistant this session) an existing, already-approved (`user_approved: true`) 2026-06-02 MineColonies wiki corpus (~60 files: `wiki-buildings-*`, `wiki-items-*`, `wiki-needs-*`). Read `wiki-needs-food-2026-06-02.md`: confirmed a systemic naive-tag-stripping defect — cross-referenced names rendered as bare lowercase Markdoc ids ("by the cook at the cook," "the baker at the baker") instead of resolved display names. Read `wiki-buildings-cook-2026-06-02.md` to confirm the pattern wasn't a one-off (self-references resolved correctly; cross-references to other buildings did not). Drew the parallel to the Minecraft pillager deprecate-and-rebuild precedent; asked whether to check more of the batch to confirm scope.
33. Dragon: **"remember, memory is dev facing. The Wiki is likely player-facing."** Correction: the defective entries found in step 32 are in the dev-facing Milvus/MemSearch corpus, not the player-facing track — real but lower-priority, and not what this task was building toward. Confirmed the actual player-facing mechanism by reading `LoreIndex.java`'s static initializer in full: loads `/data/dragontweaksv2/lore/lore-manifest.txt` plus per-entry `.md` resources bundled into the JAR, sourced from `docs/minecraft-lore/` via the `syncLoreFromDocs` Gradle task. Confirmed no MineColonies equivalent existed yet. Reframed: the stage-2 cleaned-reference work from step 30 is still the right direction, just benchmarked against the wrong artifact; asked to confirm continuing toward an actual `docs/minecolonies-lore/`-style entry.
34. Dragon: **"yes, continue."** Checked for a recorded source URL for the wiki-ref captures (none found, confirmed via Glob/Grep — no README/index in `docs/wiki-ref/`, no URL in any `.mdoc` frontmatter seen). Checked `build.gradle` for the pinned MineColonies version (`minecolonies-1.1.1299-1.21.1-snapshot.jar`). Created `docs/minecolonies-lore/needs/food.md` (new file, new directory) mirroring `pillager.md`'s exact frontmatter (`topic`/`type: advisor-artifact`/`source`/`scraped`/`version`/`pipeline_stage`) and structure (dense intro + bullet sections: Production Chain, Tiers and Vanilla Food, Saturation Mechanics, Known Gap). Marked `source`/`scraped` as "unknown — needs verification" rather than guessing. Explicitly did not wire it into the manifest/JAR without checking in; reported and asked whether to wire it in or hold.
35. Dragon: **"wire it in."** Found that `syncLoreFromDocs` (in `build.gradle`) was hardcoded to a single `docs/minecraft-lore` root and would not have found the new file under `docs/minecolonies-lore/`. Read `LoreIndexTest.java` (confirmed no hardcoded entry-count assertion that would break) and the rest of `LoreIndex.java` (confirmed matching is by literal whole-word entry-name against the player's message — no full-text search — so "food" as a match key is unproblematic, consistent with existing single-word entries). Generalized `syncLoreFromDocs` to check a list of doc roots (`docs/minecraft-lore`, `docs/minecolonies-lore`) in order, first match wins — the 91 pre-existing manifest entries are unaffected. Added `needs/food` to `lore-manifest.txt` (appended at the end, following the precedent set by the earlier `effects/`/`dimensions/` additions). Ran `./gradlew syncLoreFromDocs` — log confirmed 88 synced (was 87) / 4 skipped (unchanged — the `effects/*` entries with no docs counterpart). Read back the generated resource file to confirm an exact match against the authored source. Ran `./gradlew test` — 96 tests, 0 failures (unchanged), and confirmed via the test-run log that `LoreIndex` now loads 92 entries (was 91), i.e. the new entry is genuinely wired into the runtime path. Appended a `test-audit-trail.md` entry documenting all of this, including the dev-memory defect finding kept explicitly separate per Dragon's correction in step 33, and fixed one typo in that entry afterward.

## Files Discussed Or Modified

| File | Status |
|---|---|
| `src/test/java/.../advisor/AdvisorPersonaGenerativeTest.java` | modified (parallelized `setUpAndRunTrials`) |
| `src/main/java/.../openrouter/OpenRouterService.java` | inspected (ruled out as a bottleneck) |
| `test-audit-trail.md` | modified (2 new 2026-06-25 entries this session: test-fix, lore-wiring; one typo fix) |
| `[memory] feedback_git_access_revoked.md` | inspected, modified |
| `[memory] project_open_items.md` | inspected (earlier in broader session), modified 4 times this session (3 paused updates + 1 UTF-8 follow-up) — later edits applied from retained context, not a fresh `Read` each time |
| `claude-links/guardrail-boundary-handling.md` | inspected |
| `scripts/memsearch-refresh.ps1` | inspected, **blocked** (twice), then modified (after settings change) |
| `.claude/settings.local.json` | discussed only — opened by Dragon in the IDE per system reminders; not inspected or edited by the assistant |
| `claude-links/web-memory-ingestion-pipeline.md` | inspected |
| `docs/wiki-ref/needs/food.mdoc` | inspected |
| `docs/wiki-ref/buildings/kitchen.mdoc` | inspected |
| `docs/wiki-ref/buildings/cook.mdoc` | inspected |
| `docs/wiki-ref/buildings/baker.mdoc` | inspected |
| `docs/wiki-ref/buildings/farmer.mdoc` | inspected |
| `.memsearch/memory/domains/minecraft/approved/pillager.md` | inspected |
| `.memsearch/memory/domains/minecraft/deprecated/pillager.md` | inspected |
| `docs/minecraft-lore/hostile/pillager.md` | inspected |
| `docs/minecraft-lore/MINECRAFT_LORE_INDEX.md` | inspected (partial) |
| `.memsearch/memory/domains/minecolonies/approved/wiki-needs-food-2026-06-02.md` | inspected |
| `.memsearch/memory/domains/minecolonies/approved/wiki-buildings-cook-2026-06-02.md` | inspected |
| `src/main/java/.../advisor/LoreIndex.java` | inspected (full) |
| `src/test/java/.../advisor/LoreIndexTest.java` | inspected |
| `build.gradle` | inspected, modified (`syncLoreFromDocs` generalized to multi-root) |
| `gradle.properties` | inspected (no MineColonies version found there) |
| `docs/minecolonies-lore/needs/food.md` | **created** (new file, new directory — first MineColonies player-facing lore entry) |
| `src/main/resources/data/dragontweaksv2/lore/lore-manifest.txt` | inspected, modified (added `needs/food`) |
| `src/main/resources/data/dragontweaksv2/lore/needs/food.md` | **created** (generated by `syncLoreFromDocs`, not hand-edited) |
| `codify/codify03.md` | discussed only (superseded baseline for this snapshot) |
| `codify/codify04.md` (this file) | created |

## Violations, Corrections, And User Directives

- **"pause. do not lose progress."** — mid-task interjection during memory-update work; honored immediately, no further tool calls queued before responding, per the standing mid-task-interjection guidance.
- **"complete the memory updates, then pause again."** — explicit scope-and-stop instruction; honored exactly (completed the four updates, then stopped without proceeding to the design proposals).
- **"don't bother with altering the tests then... We need to actively resolve the time-consuming issues rather than let them linger... More effort needs to be directed as solving these issues."** — rejected the `@Tag`/exclusion proposal outright and redirected toward an actual root-cause fix; this was the basis for the parallelization fix.
- **Tool-use rejection, "fix command and retry"** — a standalone PowerShell verification command (bulk recursive UTF-8 read across all approved memory directories) was rejected; re-scoped to a narrower syntax-only check that didn't read memory content. Treated as a correction about scope (bulk memory reads), not a syntax problem, based on context.
- **"you are authorized to make that change to the memsearch-refresh script."** then **"try again"** (after opening `.claude/settings.local.json`) — explicit authorization sequence for a previously settings-deny-blocked edit; the edit was retried (not bypassed via another tool) both times, succeeding only after the underlying setting itself changed.
- **"remember, memory is dev facing. The Wiki is likely player-facing."** — direct correction reframing the significance of the dev-memory defect found in `.memsearch/memory/domains/minecolonies/approved/`; the assistant had been treating it as more urgent/relevant to the active task than it actually was.
- **"you forgot I had follow-up questions."** — correction for moving the conversation toward the next task instead of yielding for Dragon's queued question; acknowledged directly, no further action taken until the question was asked.
- No other process violations or unrequested actions occurred this session; all file-changing actions were either explicitly requested or were the directly-necessary mechanism to fulfill an explicit request (e.g. generalizing `syncLoreFromDocs` was necessary to actually fulfill "wire it in," not an independent choice).

## Decisions Made

1. Test-suite slowness root-caused to one class's sequential live-API calls (97% of JUtest time), not "many tests" — fixed via parallelization (`CompletableFuture` fan-out/fan-in over a fixed thread pool), not exclusion/tagging. Measured: suite time 76.19s→15.81s; realistic `./gradlew test` wall-clock ~97s→30.6s; 96 tests/0 failures unchanged.
2. `/codify` is not required mid-session purely to "be safe" — reserved for cross-session continuity or natural stopping points; this was Dragon's own implicit confirmation by proceeding without one after asking.
3. All four memory updates paused at the end of `codify03.md` are now complete: sharpened git-access wording (stop asking, blocked while bugs remain), UTF-8 standing requirement, domain-retrieval architecture note, wiki-ref reclassification.
4. UTF-8 enforcement: implemented (not just recorded) as a strict validation pre-check in `scripts/memsearch-refresh.ps1`, the one in-repo ingestion boundary reachable; MemSearch's own internals, auto-memory writes, and the not-yet-built in-JAR KB are each separately out of scope (external tool, already-default, doesn't exist yet, respectively).
5. A settings-level permission deny rule cannot be overridden by conversational authorization alone — confirmed by a failed retry after explicit verbal authorization; only succeeded after the underlying `.claude/settings.local.json` setting itself was changed by Dragon.
6. Verification scope for the UTF-8 check was deliberately narrowed (syntax-only, no bulk memory-content read) after the broader verification attempt was rejected; the script has not been run end-to-end (would trigger Docker + a live Milvus reindex).
7. Subagent-suitability verdicts: UTF-8 validity auditing of existing data = not a good subagent task (deterministic, mechanical, a direct decode loop beats any agent); `docs/wiki-ref/` Markdoc-to-clean-prose conversion = a good subagent task, conditioned on resolving the tag-handling convention first to avoid inconsistent parallel output.
8. Markdoc convention resolved (10-rule table) and applied to `needs/food.mdoc`, producing a stage-2 "cleaned reference."
9. The dev-facing Milvus memory corpus for MineColonies (`.memsearch/memory/domains/minecolonies/approved/`) already has approved-but-defective wiki entries (confirmed in 2 of ~60 files) with the same naming-resolution defect the new convention fixes — noted as a real but explicitly deprioritized, separate-track finding, not actioned.
10. The actual player-facing pipeline is `docs/*-lore/` → `syncLoreFromDocs` → JAR resources (`/data/dragontweaksv2/lore/`) → `LoreIndex.java` at runtime — confirmed by reading `LoreIndex.java` directly; no MineColonies equivalent existed before this session.
11. Built and fully wired in `docs/minecolonies-lore/needs/food.md` — the first MineColonies entry in that real player-facing pipeline — mirroring `docs/minecraft-lore/hostile/pillager.md`'s exact format; unknown source/scrape-date fields marked as such rather than guessed; the `{% food_list /%}` dynamic-content gap carried forward explicitly rather than silently dropped.
12. `syncLoreFromDocs` generalized from a single hardcoded doc root to a list of roots, checked in order — the 91 pre-existing manifest entries are unaffected; this was necessary (not optional) to fulfill "wire it in" at all, since the task could not otherwise find a file outside `docs/minecraft-lore/`.

## Deferred / Not Yet Implemented

- The two design-only proposals from before `codify03.md` (#5 "see" keyword overlap; #6 persona 4th-wall slip, target ambiguous) — untouched this entire session, no new information.
- In-game live confirmation of: the cavern-classification fix, persona list-formatting fix, build-tool spawn gate (all pre-`codify03.md`), and now also the new `needs/food` lore entry (does asking the advisor about colony food actually trigger correct injection in a running client?).
- Confirming the scope of the dev-memory MineColonies defect across the full ~60-file 2026-06-02 batch (only 2 files checked) — and deciding whether/when to deprecate-and-rebuild that corpus, mirroring the Minecraft pillager precedent. Explicitly deprioritized relative to the player-facing track, not abandoned.
- Running `scripts/memsearch-refresh.ps1` end-to-end to confirm the new UTF-8 validation check actually behaves correctly against the live Docker/Milvus pipeline.
- An actual audit of existing memory/KB data for UTF-8 validity — explicitly decided to do directly/mechanically (not via subagents) whenever it happens, but not yet done.
- Scaling the resolved Markdoc convention to the remaining 61 `docs/wiki-ref/` source files (`buildings/` ×49, `items/` ×1 more, `needs/` ×3 more, `systems/` ×8) toward more `docs/minecolonies-lore/` entries — identified as a good subagent-dispatch candidate once the convention is considered sufficiently proven on this first file, but not yet started at scale.
- Formally writing the resolved Markdoc convention into a durable memory/process file (mirroring `process_web_memory_ingestion_pipeline.md`) — currently exists only in this conversation and this codify snapshot, not as its own memory entry.
- Whether to build a `MINECOLONIES_LORE_INDEX.md` navigation index (mirroring `MINECRAFT_LORE_INDEX.md`) — not raised as a decision point yet; reasonable to defer until more than one entry exists.

## Carry-Forward Context

Two largely independent threads are open simultaneously: the original advisor-system design proposals (#5/#6, dormant since `codify03.md`, no new information — a future session should not assume they've been forgotten, just genuinely not yet reached), and the new MineColonies lore-pipeline build-out (active, with real momentum and one proven entry). These are not in tension and can be worked in either order.

For the lore-pipeline thread specifically: the hard part (resolving the Markdoc convention, discovering the dev-memory-vs-player-facing distinction, building the generalized `syncLoreFromDocs` mechanism) is done. The remaining work — applying the same 10-rule convention to 61 more `.mdoc` files — is comparatively mechanical *application* of an already-resolved convention, which is exactly the profile Dragon and the assistant agreed is a good subagent-dispatch candidate. A future session should not re-derive the convention from scratch; it should locate this codify file's Session Topics Developed #29–30 (the 10-rule table) and `docs/minecolonies-lore/needs/food.md` (the worked example) and either continue manually or actually dispatch the subagent batch that was discussed but not yet executed. Building names must be cross-referenced against each `buildings/*.mdoc`'s own `name:`/`plural:` frontmatter (grounded); worker names have no such registry and must be flagged as inferred Titlecase, not asserted as fact.

The dev-memory defect in `.memsearch/memory/domains/minecolonies/approved/` is real and confirmed (not hypothetical) but is a separate, lower-priority, dev-facing-only track per Dragon's explicit correction — a future session should not conflate it with or treat it as blocking the player-facing lore work, and should not assume Dragon wants it fixed without being asked.

The permission-boundary episode this session (deny rule on `scripts/`, blocked twice, lifted only via an actual settings change) is a useful precedent: conversational authorization from Dragon is necessary but not sufficient to clear a settings-level deny rule — if a future session hits the same kind of block, the correct sequence is report → ask Dragon to adjust the setting directly (not request an exception) → retry only after, never attempt a same-action workaround via a different tool.

Both `test-audit-trail.md` entries from this session are written and in place; nothing about this session's work is uncommitted-but-undocumented. Git access remains revoked, now with a sharper, no-longer-tentative standing instruction not to raise it at all.

## Next Recommended Action

Live-confirm the new `docs/minecolonies-lore/needs/food.md` entry actually works in a running client (ask the advisor about colony food and verify the lore injection fires correctly) before deciding whether to scale the now-resolved Markdoc convention out to the remaining 61 `docs/wiki-ref/` files via subagents.
